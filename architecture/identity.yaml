container:
  id: identity
  description: Authentication primitives — broker secret management, key hashing, verification

modules:
  - id: secret
    file: secret.rs
    files:
      - mod.rs
    description: Broker secret file management, SHA-256 key hashing, hash verification
    owns: [ensure_broker_secret, hash_key, verify_key_hash]
