ARCH: 0.4
CONT: identity
PATH: broker/src/identity
DEP:
DESC: Authentication primitives — key hashing, hash verification, Ed25519 signature verification

MOD: secret
  FILES: mod.rs; secret.rs
  OWN: ensure_broker_secret; hash_key; verify_key_hash
  BND: Pure crypto — no network, no DB, no stanza concerns

MOD: ed25519
  FILE: ed25519.rs
  OWN: build_challenge_payload; verify_agent_signature; VerifyError
  BND: Pure crypto — deterministic, no state, no randomness, no DB
